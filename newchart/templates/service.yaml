apiVersion: v1
kind: Service
metadata:
  name: {{.Values.name}}-app
spec:
  type: ClusterIP
  selector:
    app: {{.Values.name}}-app
  ports:
    - name: http
      port: {{.Values.port}}
      targetPort: {{.Values.port}}